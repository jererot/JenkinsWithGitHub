@model CristoRey.Models.MATRICULA

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>MATRICULA</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayName("Alumno")
        </dt>

        <dd>
            @Html.DisplayFor(model => model.ALUMNO.nom_alum)
        </dd>

        <dt>
            @Html.DisplayName("Grado")
        </dt>

        <dd>
            @Html.DisplayFor(model => model.GRADO.des_gra)
        </dd>

        <dt>
            @Html.DisplayName("Promedio Bimestral")
        </dt>

        <dd>
            @Html.DisplayFor(model => model.pro_mat)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
